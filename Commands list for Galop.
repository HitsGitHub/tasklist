--> changes on environment.prod.ts file.
     base_url: "http://test.galop.be/web/api"
--> generate .htaccess file & put on src/ folder
--> then give the path in angular.json file like belove
    "assets": [
              "src/favicon.ico",
              "src/assets",
              "src/.htaccess"

--> for Crete htaccess file:
     paste SSH key on Terminal: ssh testgalopbe@.....
     then.....go to dir/..cd www/galop  & check is there any .htacces file??
     if no file exist then-->> fire command on www.galop path.. ls -la
     copy code...RewriteEngine on
     #-----
     RewriteCone------
     $ then create .htaccess file on src/.htacces then Paste there. 


sshKEY : ssh testgalopbe@ssh.test.galop.be FxW9gHGfdmRJBVmF 

ng build --prod --base-href=/galop/
sudo scp -r dist/* testgalopbe@ssh.test.galop.be/data/sites/web/testgalopbe/www/galop

